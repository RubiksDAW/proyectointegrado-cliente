import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bikebros.app',
  appName: 'bikeBros',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
