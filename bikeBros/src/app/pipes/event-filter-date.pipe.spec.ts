import { EventFilterDatePipe } from './event-filter-date.pipe';

describe('EventFilterDatePipe', () => {
  it('create an instance', () => {
    const pipe = new EventFilterDatePipe();
    expect(pipe).toBeTruthy();
  });
});
