import { RouteFilterDistancePipe } from './route-filter-distance.pipe';

describe('RouteFilterDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new RouteFilterDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
