import { RouteFilterLevelPipe } from './route-filter-level.pipe';

describe('RouteFilterLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteFilterLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
